server:
  port: 8001
spring:
  application:
    name: yixue-admin
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 1.12.236.238:8848
        namespace: dev
      config:
        server-addr: 1.12.236.238:8848
        #文件后缀
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev

#  main:
#    # 允许相同名称的bean覆盖
#    allow-bean-definition-overriding: true
